# 时间序列录像功能使用说明

## 功能概述

时间序列录像功能允许用户根据不同场景类型来录制和处理视频，生成时间序列合成图片。

## 主要组件

### 1. 场景类型 (SceneType)
- **物体变化**: 适用于记录物体状态变化（如花朵盛开、物品移动、日出日落、植物生长、建筑施工等）
- **人物动作**: 适用于记录人物动作序列（如瑜伽、健身、化妆、跳舞等）
- **运动轨迹**: 适用于记录快速运动轨迹（如投篮、滑板、跳跃等运动）

### 2. 使用流程

#### 2.1 启动时间序列模式
1. 在主录像界面，点击顶部的模式切换器
2. 选择想要的场景类型
3. 阅读拍摄指导说明
4. 开始录制或选择已有视频

#### 2.2 录制视频
- 根据场景指导进行录制
- 建议保持设备稳定
- 确保主要变化在画面中央

#### 2.3 视频处理
1. 选择或录制完视频后，进入时间序列处理界面
2. 调整关键帧数量（默认5帧）
3. 点击"生成时间序列图片"按钮
4. 等待处理完成
5. 查看生成的合成图片

### 3. 功能开关

可以通过 `TimeSequenceModeManager.isTimeSequenceModeEnabled` 来控制是否启用时间序列功能：

```swift
// 启用功能
TimeSequenceModeManager.isTimeSequenceModeEnabled = true

// 禁用功能
TimeSequenceModeManager.isTimeSequenceModeEnabled = false

// 在 MainCameraViewController 中切换功能
mainCameraVC.toggleTimeSequenceFeature()
```

### 4. 技术细节

#### 4.1 场景参数配置
每个场景类型都有预设的处理参数：
- 帧提取间隔
- 关键帧数量
- 处理算法类型

#### 4.2 视频处理流程
1. 视频帧提取
2. 关键帧选择
3. 图像合成
4. 结果展示

## 界面说明

### 主录像界面
- 模式切换器：显示当前模式（普通录像/时间序列）
- 点击可切换到时间序列模式

### 场景选择界面
- 展示所有可用的场景类型
- 每个场景有详细说明和推荐用途

### 拍摄指导界面
- 针对所选场景的具体拍摄建议
- 包含技巧和注意事项

### 时间序列处理界面
- 视频预览
- 参数调整
- 实时进度显示
- 结果展示

## 测试建议

### 推荐测试场景

#### 🍞 物体变化场景（推荐新手）
**测试示例：**
- 放一个苹果，慢慢移动位置（8-15秒）
- 面包发酵过程（加速录制）
- 花朵开放过程
- 杯子里的水位变化
- 积木搭建过程

**预期效果：** 对比度+20%，饱和度+10%，突出物体变化细节

#### 🧘‍♀️ 人物动作场景
**测试示例：**
- 简单手势变换（10-20秒）
- 瑜伽基础动作
- 化妆步骤记录
- 健身动作分解
- 舞蹈动作序列

**预期效果：** 对比度+5%，亮度+5%，保持肤色自然

#### 🏀 运动轨迹场景
**测试示例：**
- 扔球轨迹（5-12秒）
- 挥手动作
- 跳跃过程
- 投篮动作
- 滑板技巧

**预期效果：** 专门锐度增强，捕捉快速运动细节

### 测试流程
1. 选择上述任一场景进行录制
2. 确保按照时长建议录制
3. 进入时间序列处理界面
4. 观察是否成功提取5个关键帧
5. 检查处理后的图片效果

### 测试验证点
- ✅ 视频文件能否正常加载
- ✅ 帧提取是否成功（应该有5帧）
- ✅ 图片处理效果是否符合场景特点
- ✅ 处理时间是否合理（通常10-30秒）
- ✅ 结果图片是否清晰可用

## 注意事项

1. **设备稳定性**: 录制时尽量保持设备稳定，避免晃动
2. **光线条件**: 确保光线充足且稳定
3. **变化焦点**: 确保主要变化在画面中央
4. **时间间隔**: 根据场景类型选择合适的录制时长
5. **存储空间**: 处理大视频文件时需要足够的存储空间

## 故障排除

### 处理失败
- 检查视频文件是否完整
- 确保有足够的存储空间
- 尝试减少关键帧数量

### 界面异常
- 重启应用
- 检查功能开关状态
- 确认权限设置正确

## 技术细节补充

### 图片处理算法详解

#### 物体变化场景处理
```swift
private func enhanceContrast(_ image: UIImage) -> UIImage {
    // 使用 CIColorControls 滤镜
    filter?.setValue(1.2, forKey: kCIInputContrastKey)     // 对比度+20%
    filter?.setValue(1.1, forKey: kCIInputSaturationKey)   // 饱和度+10%
}
```

#### 人物动作场景处理
```swift
private func balanceColors(_ image: UIImage) -> UIImage {
    // 保持自然色彩，适合人物拍摄
    filter?.setValue(1.05, forKey: kCIInputContrastKey)    // 对比度+5%
    filter?.setValue(1.0, forKey: kCIInputSaturationKey)   // 保持原始饱和度
    filter?.setValue(0.05, forKey: kCIInputBrightnessKey)  // 亮度+5%
}
```

#### 运动轨迹场景处理
```swift
private func enhanceSharpness(_ image: UIImage) -> UIImage {
    // 专门的锐度增强算法，适合快速运动
    // 使用 CIUnsharpMask 或自定义锐化滤镜
}
```

### 帧提取策略

- **物体变化**: 均匀分布取帧 - 适合记录渐变过程
- **人物动作**: 加权取帧 - 侧重动作关键时刻  
- **运动轨迹**: 密集取帧 - 捕捉快速变化细节

每种场景默认提取5个关键帧，可根据视频时长和内容自动调整间隔。

## 更新日志

- v1.0: 基础时间序列功能实现
  - 场景类型定义（3种场景）
  - 模式管理和切换
  - 视频处理和帧提取
  - 界面集成和用户指导
  - 图片处理算法优化
