# 批量修复图片对应关系测试指南

## 测试目的

验证批量修复后，点击单个图片进入详情页面时，修复前后的图片对应关系是否正确。

## 测试前准备

1. 准备至少 5 张以上的测试图片
2. 建议图片内容有明显区别（如不同场景、颜色等），便于识别对应关系

## 测试步骤

### 测试 1：连续图片批量修复

**目标**：测试选择连续图片的对应关系

1. 进入批量修复页面
2. 选择前 3 张图片（索引 0, 1, 2）
3. 选择"中度"修复等级
4. 点击"开始修复"
5. 等待修复完成
6. **验证步骤**：
   - 点击第 1 张图片，查看修复前后对比
   - 确认"修复前"显示的是第 1 张原图
   - 确认"修复后"显示的是第 1 张的修复结果
   - 返回，点击第 2 张图片，重复验证
   - 返回，点击第 3 张图片，重复验证

### 测试 2：不连续图片批量修复（关键测试）

**目标**：测试选择不连续图片的对应关系（这是最容易出问题的场景）

1. 进入批量修复页面
2. 先点击"全选"，再点击"反选"清空选择
3. **只选择第 2、5、8 张图片**（不连续的索引）
4. 选择"重度"修复等级
5. 点击"开始修复"
6. 等待修复完成
7. **验证步骤**：
   - 点击第 2 张图片（索引 1）
     - 确认左侧"修复前"是第 2 张原图
     - 确认右侧"修复后"是第 2 张的修复结果
   - 返回，点击第 5 张图片（索引 4）
     - 确认左侧"修复前"是第 5 张原图
     - 确认右侧"修复后"是第 5 张的修复结果
   - 返回，点击第 8 张图片（索引 7）
     - 确认左侧"修复前"是第 8 张原图
     - 确认右侧"修复后"是第 8 张的修复结果

### 测试 3：边界情况测试

**目标**：测试极端情况的对应关系

1. **测试最后一张**：
   - 只选择最后一张图片
   - 点击修复
   - 验证对应关系正确

2. **测试第一张**：
   - 只选择第一张图片
   - 点击修复
   - 验证对应关系正确

3. **测试全部图片**：
   - 选择所有图片
   - 点击修复
   - 随机点击几张验证对应关系正确

## 查看调试日志

如果需要更详细的验证，可以在 Xcode 控制台查看日志输出：

### 批量修复时的日志

```
🔄 开始处理索引2的图片，原图尺寸=(width, height)
✅ 索引2修复成功，修复图尺寸=(width, height)
🔄 开始处理索引5的图片，原图尺寸=(width, height)
✅ 索引5修复成功，修复图尺寸=(width, height)
🔄 开始处理索引8的图片，原图尺寸=(width, height)
✅ 索引8修复成功，修复图尺寸=(width, height)
```

### 点击单个图片时的日志

```
✅ showDetailView: 打开索引2的图片，状态=completed, 是否有修复图=true
✅ 创建单图修复页面: 已修复状态，索引=2
✅ ImageEnhanceVC.setEnhancedImage: 设置预设修复图片，尺寸=(width, height)
✅ ImageEnhanceVC.viewDidLoad: 原始图片尺寸=(width, height)
✅ ImageEnhanceVC.viewDidLoad: 批量模式，当前索引=2, 总数=10
✅ ImageEnhanceVC.viewDidLoad: 发现预设修复图片，尺寸=(width, height)
```

## 判断标准

### ✅ 测试通过标准

1. 每张图片的"修复前"和"修复后"在内容上明确对应
2. 修复前后的图片尺寸一致
3. 控制台日志显示的索引和图片尺寸信息一致
4. 没有出现黑屏或显示错误图片的情况

### ❌ 测试失败标准

1. "修复前"显示的不是应该显示的原图
2. "修复后"显示的不是对应图片的修复结果
3. 修复前后的图片内容明显不匹配
4. 出现黑屏、闪烁或加载错误

## 常见问题排查

### 问题 1：修复后的图片对应关系错误

**现象**：点击第 2 张图片，但显示的是第 1 张图片的修复结果

**排查**：
- 查看控制台日志，确认 `originalIndex` 是否正确
- 确认是否使用了最新修复后的代码

### 问题 2：显示黑屏或图片加载失败

**现象**：点击图片后，修复前或修复后区域显示黑色

**排查**：
- 检查 `comparisonView` 的设置是否正确
- 确认图片尺寸信息在日志中是否正常

### 问题 3：图片尺寸不一致

**现象**：修复前后的图片尺寸显示不同

**排查**：
- 查看日志中的图片尺寸信息
- 确认 `ImageEnhancer` 是否改变了图片尺寸

## 测试报告模板

测试完成后，请按以下格式记录结果：

```
测试日期：____年__月__日
测试人员：________

测试 1 (连续图片)：
- 第 1 张：✅ 通过 / ❌ 失败
- 第 2 张：✅ 通过 / ❌ 失败
- 第 3 张：✅ 通过 / ❌ 失败

测试 2 (不连续图片)：
- 第 2 张：✅ 通过 / ❌ 失败
- 第 5 张：✅ 通过 / ❌ 失败
- 第 8 张：✅ 通过 / ❌ 失败

测试 3 (边界情况)：
- 最后一张：✅ 通过 / ❌ 失败
- 第一张：✅ 通过 / ❌ 失败
- 全部图片：✅ 通过 / ❌ 失败

总体评价：✅ 全部通过 / ⚠️ 部分失败 / ❌ 未通过

备注：
（记录任何异常情况或改进建议）
```

## 注意事项

1. **测试环境**：建议在真机上测试，模拟器可能有性能差异
2. **图片选择**：使用内容明显不同的图片，便于识别对应关系
3. **日志记录**：如果发现问题，务必保存完整的控制台日志
4. **重复测试**：如果发现问题，重启 App 后重新测试，确认是否可复现

## 修复效果验证

### 修复前（问题现象）

- 选择第 2、5、8 张图片批量修复
- 点击第 2 张图片，可能显示第 1 张的修复结果 ❌
- 点击第 5 张图片，可能显示第 2 张的修复结果 ❌
- 点击第 8 张图片，可能显示第 3 张的修复结果 ❌

### 修复后（预期行为）

- 选择第 2、5、8 张图片批量修复
- 点击第 2 张图片，显示第 2 张的修复结果 ✅
- 点击第 5 张图片，显示第 5 张的修复结果 ✅
- 点击第 8 张图片，显示第 8 张的修复结果 ✅


